# Database Script 0x01 - Schema Design

## Overview
This directory contains the Data Definition Language (DDL) scripts to initialize the DataScape database schema.

## How to use
1. Ensure you have a PostgreSQL-compatible database.
2. Run the script using your SQL client:
   ```bash
   psql -d airbnb_db -f schema.sql